<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- 自定义菜单溢出按钮样式 -->
        <item name="actionOverflowButtonStyle">@style/Widget.AppCompat.ActionButton.Overflow</item>
        <item name="android:actionOverflowButtonStyle">@style/Widget.AppCompat.ActionButton.Overflow</item>
        <item name="actionOverflowMenuStyle">@style/Widget.AppCompat.PopupMenu</item>
        <item name="android:actionOverflowMenuStyle">@style/Widget.AppCompat.PopupMenu</item>
        
        <!-- EditText hint颜色 -->
        <item name="android:editTextColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item>
    </style>
    <style name="Theme.NewNote" parent="Base.Theme.NewNote" />
    
    <!-- 自定义菜单溢出按钮样式 -->
    <style name="Widget.AppCompat.ActionButton.Overflow" parent="Widget.AppCompat.ActionButton">
        <item name="android:src">@android:drawable/ic_menu_more</item>
        <item name="android:contentDescription">更多选项</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">16dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:tint">#000000</item>
        <item name="android:alpha">1.0</item>
        <item name="android:elevation">8dp</item>
    </style>
</resources>